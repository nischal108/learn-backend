<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdn.jsdelivr.net/npm/remixicon/fonts/remixicon.css" rel="stylesheet">
</head>

<body class="bg-gray-900 text-white">
    <nav class="bg-gray-800 p-6">
        <div class="container mx-auto">
            <div class="flex items-center justify-between">
                <div class="flex items-center flex-shrink-0 text-white mr-6">
                    <span class="font-semibold text-xl tracking-tight">My Blog</span>
                </div>
                <div class="block lg:hidden">
                    <button
                        class="flex items-center px-3 py-2 border rounded text-gray-200 border-gray-400 hover:text-white hover:border-white">
                        <i class="ri-menu-line text-2xl"></i>
                    </button>
                </div>
                <div class="hidden lg:block">
                    <div class="flex items-center">
                        <a href="/post/create"
                            class="block mt-4 lg:inline-block lg:mt-0 text-gray-200 hover:text-white mr-4">
                            <i class="ri-add-line mr-1"></i>Create Post
                        </a>
                        <a href="/profile"
                            class="block mt-4 lg:inline-block lg:mt-0 text-gray-200 hover:text-white mr-4">
                            <i class="ri-account-circle-line mr-1"></i>Profile
                        </a>
                        <a href="/logout" class="block mt-4 lg:inline-block lg:mt-0 text-gray-200 hover:text-white">
                            <i class="ri-logout-box-line mr-1"></i>Logout
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </nav>

    <div class="container mx-auto py-10">
        <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
            <% posts.forEach(post=> { %>
                <div class="bg-gray-800 rounded-lg overflow-hidden shadow-lg">
                    <img class="w-full" src="<%= post.image %>" alt="<%= post.title %>">
                    <div class="p-6">
                        <h2 class="text-xl font-semibold mb-2">
                            <%= post.title %>
                        </h2>
                        <p class="text-gray-400">
                            <%= post.description %>
                        </p>
                        <p class="text-gray-400">
                            <%= author.name %>
                        </p>
                        <a href="/post/<%= post._id %>"
                            class="block mt-4 bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">Read
                            more</a>
                    </div>
                </div>
                <% }) %>
        </div>
    </div>
</body>

</html>